import { EstadoCivilDirective } from './estado-civil.directive';

describe('EstadoCivilDirective', () => {
  it('should create an instance', () => {
    const directive = new EstadoCivilDirective();
    expect(directive).toBeTruthy();
  });
});
